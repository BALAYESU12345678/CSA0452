#include <stdio.h>

int main() {
    int n, i;
    int bt[10], wt[10], tat[10];
    float avg_wt = 0, avg_tat = 0;

    printf("Enter number of processes: ");
    scanf("%d", &n);

    for(i = 0; i < n; i++) {
        printf("Burst time: ");
        scanf("%d", &bt[i]);
    }

    wt[0] = 0;

    for(i = 1; i < n; i++) {
        wt[i] = wt[i-1] + bt[i-1];
    }

    for(i = 0; i < n; i++) {
        tat[i] = wt[i] + bt[i];
        avg_wt += wt[i];
        avg_tat += tat[i];
    }

    printf("\nWT\tTAT\n");
    for(i = 0; i < n; i++) {
        printf("%d\t%d\n", wt[i], tat[i]);
    }

    printf("Avg WT = %.2f\n", avg_wt/n);
    printf("Avg TAT = %.2f\n", avg_tat/n);

    return 0;
}
